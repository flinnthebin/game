#include <iostream>
#include <queue>

class RenderManager() {
  private:
    // TODO: create the priority queue to house the managers
  public:
    RenderManager() {
      // do nothing - bruteforce method
    }
    ~RenderManager() {
      // do nothing - bruteforce method
    }

    void startUp() {
      //TODO: start up the manager (elements of priority queue)
    }

    void shutDown() {
      //TODO: shut down the manager (reverse priority queue)
    }
}
